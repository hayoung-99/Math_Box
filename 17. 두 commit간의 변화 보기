두 commit 간의 변화를 알고 싶은 경우, commit 메시지들만으로는 변화가 뭔지 정확히 알 수 없다.
이런 경우 git diff (두 commit의 id) 커맨드를 입력하면 두 commit 사이에서 무슨 변화가 있는지를 알 수 있다.

예를 들어 비교할 두 commit 중, 이전 commit이 35fa~ id로 시작하고, 이후 commit이 224a~ id로 시작한다면
git diff 35fa 224a를 치면 된다.

그러면 뜨는 결과창에는 -로 시작하는 부분이 '이전 commit의 모습', +로 시작하는 부분이 '이후 commit의 모습'을 나타낸다. 